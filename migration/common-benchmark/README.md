# Common-benchmark migration preparation

**Inspection only; PR 6 remains draft.** Benchmark selection is pending a candidate-blind source comparison among incumbent v3, the native benchmark, and a possible independently reviewed v4. The older v3 mandate does not establish superiority. This reversible inventory analyzes the conditional case of migration to the incumbent legacy v3 release at `98dbffd0ca171b5b7db76dbe1b2b5d5265ccacab`. Its file SHA-256 is `34a399cda8ca9f1b07b9fa0ddad36ac4f5073ef12d8b12df42fb023818508b27`, canonical SHA-256 `b925797fcab50b2008ad5974590e323f772e5ea7013efa84ce7606007439aeb3`. Inspection verified the file against the frozen Git blob. The final V8.2 migration and its private archive remain unchanged.

The current native benchmark has 638 subjects, 1,569 coalesced treatments, 638 tasks and 281 relationships. The incumbent v3 release has 1,366 subjects, 3,210 treatments, 1,026 tasks and 3,460 relationships. They have no shared stable subject IDs. Moving between these releases would be a substantive benchmark change: **97.19 is the preserved historical score, not a result on the common benchmark**, and score/cap invariance would not be required if a substantively different benchmark is chosen. No common benchmark has yet been selected.

All rebuild/reuse dispositions below are conditional planning, not authorization to bind or migrate. Hold execution until the source comparison and final benchmark decision.

The [dependency review](dependency-review.json) records input identities and the specific findings to retain or reassess. It is a static review receipt; `evaluation/evaluation-state.json` remains the only workflow inventory.

| Evidence or output | Disposition |
|---|---|
| Exact source PDF, page map, manifest and 51 source-chunk artifacts | Reusable preparation. Source/map/chunk identities match the v3 comparison baseline. Confirm common eligible-region scope before reusing substantive findings. |
| Three candidate normalization/layout/inventory artifacts; 17 locator packets | Reusable mechanical evidence. Candidate bytes, hierarchy, IDs and page ownership remain unchanged. The state-level candidate-to-benchmark binding needs the supported migration command. |
| Seventeen delivered-locator audit files, 4,273 judgments | Reusable source evidence under identical scope. The files bind the candidate, not a benchmark hash. Preserve original IDs and evidence; scope-sensitive rationale must be handled explicitly. |
| Seventeen missing-access audit files | Rebuild all deterministic ownership/worksets and assess the new subject, treatment and task obligations. Every old ledger binds the old benchmark and ownership hash. A separately approved semantic crosswalk may support bounded reuse; label matching alone does not. |
| Candidate denominators and physical reference/mechanics observations | Retain evidence for 2,292 nodes, 1,781 locator-bearing paths, 257 delivered references, and the frozen delivered-Markdown representation. No original producer-PDF typography claim is added. |
| Structure ledger | Reconstruct selectively. Physical destination existence is distinct from warranted missing routes. Reassess subject access, task routes, optional-subject decisions, applicability, dependent defects and the whole-index access pass attestation. |
| Policy, discovery/review/import provenance | Preserve historical artifacts; install the v3 comparison baseline through the supported candidate-visible retrospective workflow. Do not reuse the native discovery chain as provenance for a future selected release or reset visibility. |
| Density | Preserve candidate locator/path counts, but await a declared common per-chunk source-word basis. Native 195,346 and legacy-family 194,718 totals differ. Rebuild relevant density records and uncertainty after that decision. |
| All five scoring and five web-report artifacts | Recompute components, scores, caps, gates, diagnostics, validity, readiness and public collections. Prior report/privacy/website closure receipts apply to the old bundle. |

## Structure findings inspected

All ten numeric architecture triggers have candidate/source reviews: seven nondefects and three confirmed subdivision findings (`HAF-ARCH-0001`, `HAF-ARCH-0004`, `HAF-ARCH-0010`). Their original evidence is reusable. Three related `SUB` defect groups and four delivered-locator defect groups are independent evidence candidates, not pre-approved final consequences.

Twelve of the fourteen exception nodes have benchmark-dependent causal findings. Danton is mixed: retain its independent subdivision evidence while reassessing its old subject/task/treatment omission. The other two architecture-only nodes remain reusable evidence. Nine of the sixteen grouped defects depend on the old subject population and must be rebuilt; the receipt lists every affected finding/node/defect ID.

All 257 delivered cross-references have a supported physical-destination attestation with no exceptions. The missing warranted route for old `SUBJ-0191`, its `HAF-XREF-0009` finding and cross-reference applicability obligation are separate benchmark-dependent evidence. They cannot be carried to a new subject merely because the delivered reference count stays the same. The 104 optional-subject scoring decisions and currently empty explicit node-applicability overrides also need reassessment.

## Scope and uncertainty caveats

The old policy and map adjudication evaluate captions while excluding graphical internals. The legacy policy explicitly mentions substantive maps/captions. The shared source bytes do not by themselves establish identical region interpretation. Preserve and review the four bound assignments on pages 3, 127, 129 and 225: `LOC-C7EC8635B5EF`, `LOC-21F8A6403C4A`, `LOC-2B74EB795674`, `LOC-6EBA9C3F158C`. The page-127 rationale explicitly references the old benchmark. Do not silently reinterpret that rationale or change its judgment to force compatibility.

The source-extent and delivered-representation limitations remain applicable. The density uncertainty must follow the declared common measurement basis. The old `SUBJ-0494`/`TASK-0494` first-lookup uncertainty must remain in history and be reassessed against current obligations; it cannot be renamed or declared resolved by migration. Rebuild the current uncertainty-scope supplement from the selected current findings.

The 36 normalized-label pairs from the coordinator's crosswalk preflight are **unapproved seeds**. They authorize no judgment transfer. No fresh candidate blindness is claimed by this candidate-exposed task.

## Execution boundary and checks

Installed methodology `c11c6ccb` allows `import-reviewed-legacy` only in an unexposed source-only state. It does not authorize replacing this completed evaluation in place. The methodology owner has been asked for a candidate-visible transition with exact old/new provenance, safe dependent-stage invalidation, explicit evidence reuse and study-level compatibility checks. Both the final benchmark choice and the reviewed supported workflow are prerequisites to changing canonical state. Neither is supplied by this inventory.

Remote references were fetched without checking out or resetting any files; the starting branch matched its remote at `17c211a` and had no incoming main commits. The current state and all 158 registered artifacts were checked against the final V8.2 private archive; the local and durable saved archive copies both retain SHA-256 `664a87c0ebbb8a8f07c237136c5f2fcc959d826fd3e452db1b9725bad9133f65`. Both benchmark populations and treatment counts were independently counted, and source page-map/manifest bytes match exactly. Preparation changes only this documentation and its receipt. No benchmark import, audit rewrite, calculation, merge, activation or deployment has occurred.
